<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="net.yuanmomo.searchrescue.web.mapper.ITestTableDAO">
	<cache />
	<insert id="add" parameterType="Test" useGeneratedKeys="true"
		keyProperty="id">
		INSERT INTO testtable (NAME,DATE)
		VALUES(#{name},#{birthday})
	</insert>
	<update id="update" parameterType="Test">
		update testtable set name =
		#{name}, date=#{birthday} where id=#{id}
	</update>
	<select id="get" parameterType="int" resultType="Test">
		select id as id,
		name as name, date as birthday
		from testtable where id=#{id}
	</select>
	<select id="getForUpdate" parameterType="int" resultType="Test">
		select
		id as id, name as name, date as birthday
		from testtable where id=#{id}
		for update
	</select>
</mapper>